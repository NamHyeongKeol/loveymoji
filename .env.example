# Update this connection string to point at your Postgres instance
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/loveymoji?schema=public"

# Storage directory for uploaded files. Defaults to public/uploads
UPLOAD_DIR="public/uploads"
